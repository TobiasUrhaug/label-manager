# Invoice Parser
INVOICE_PARSER_URL=http://localhost:8000
INVOICE_PARSER_API_KEY=dev-secret-key

# Database (defaults to local dev values if not set)
DATABASE_URL=jdbc:postgresql://localhost:5432/labelmanager
DATABASE_USERNAME=dev
DATABASE_PASSWORD=dev

# S3 / MinIO (defaults to local dev values if not set)
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=costs
S3_REGION=us-east-1
S3_ACCESS_KEY=dev
S3_SECRET_KEY=devdevdev
