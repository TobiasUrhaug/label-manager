/**
 * Sales' published surface for distributor returns: the command and query APIs and the records they
 * return. Everything else in {@code sales} is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.sales.distributorreturn.api;

import org.springframework.modulith.NamedInterface;
