/**
 * Sales' published surface for sales: the command and query APIs and the records they return.
 * Everything else in {@code sales} is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.sales.sale.api;

import org.springframework.modulith.NamedInterface;
