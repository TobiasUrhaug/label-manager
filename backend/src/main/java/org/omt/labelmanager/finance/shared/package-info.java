/**
 * The document-storage port finance defines and someone else implements, with the records and
 * exception it declares. Published because the adapter lives in {@code infrastructure}: the
 * dependency runs {@code infrastructure -> finance}, one way (§5.2 rule 5).
 */
@NamedInterface("api")
package org.omt.labelmanager.finance.shared;

import org.springframework.modulith.NamedInterface;
