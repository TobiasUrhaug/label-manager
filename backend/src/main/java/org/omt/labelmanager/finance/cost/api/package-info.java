/**
 * Finance's published surface for costs: the command and query APIs and the exceptions they throw.
 * Everything else in {@code finance} is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.finance.cost.api;

import org.springframework.modulith.NamedInterface;
