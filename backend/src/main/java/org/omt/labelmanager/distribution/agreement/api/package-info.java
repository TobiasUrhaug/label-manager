/**
 * Distribution's published surface for pricing agreements: the command and query APIs and the
 * records they return. Everything else in {@code distribution} is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.distribution.agreement.api;

import org.springframework.modulith.NamedInterface;
