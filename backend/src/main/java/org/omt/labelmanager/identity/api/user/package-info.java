/**
 * Identity's published surface: the command and query APIs, {@code AppUserDetails} — the
 * authenticated principal every controller receives — and the exceptions they throw. Everything
 * else in {@code identity} is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.identity.api.user;

import org.springframework.modulith.NamedInterface;
