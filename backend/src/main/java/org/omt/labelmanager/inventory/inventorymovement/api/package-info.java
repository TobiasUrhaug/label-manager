/**
 * Inventory's published surface for the movement ledger: the command and query APIs and the records
 * they return. Everything else in {@code inventory} below the module root is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.inventory.inventorymovement.api;

import org.springframework.modulith.NamedInterface;
