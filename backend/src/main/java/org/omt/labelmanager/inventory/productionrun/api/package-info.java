/**
 * Inventory's published surface for production runs: the command and query APIs and the records
 * they return. Everything else in {@code inventory} below the module root is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.inventory.productionrun.api;

import org.springframework.modulith.NamedInterface;
