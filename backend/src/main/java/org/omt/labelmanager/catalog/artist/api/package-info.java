/**
 * Catalog's published surface for artists: the command and query APIs and the exceptions they
 * throw. Everything else in {@code catalog} is internal.
 */
@NamedInterface("api")
package org.omt.labelmanager.catalog.artist.api;

import org.springframework.modulith.NamedInterface;
