invoice:
  parser:
    url: http://test-invoice-parser
    api-key: test-api-key

storage:
  s3:
    endpoint: http://localhost:9000
    bucket: test-bucket
    region: us-east-1
    access-key: test
    secret-key: testtest
