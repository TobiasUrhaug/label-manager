{
  "name": "label-manager-contracts",
  "private": true,
  "type": "module",
  "description": "The OpenAPI contract, plus the check that it parses and resolves.",
  "scripts": {
    "validate": "node validate.mjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
